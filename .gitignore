# Secrets
.env
dashboards/.env

# Proposal classification cache — safe to commit (public data, no secrets)
!dashboards/scripts/.proposal_cache.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
ENV/


# Exception: dbt seed CSVs are committed
!infra/dbt/seeds/*.csv

# Dagster runtime (auto-generated logs/telemetry — not source code)
.dagster/logs/
.dagster/.logs_queue/
.dagster/.nux/
.dagster/.telemetry/
.tmp_dagster_home_*
# .dagster/dagster.yaml and .dagster/storage/ are tracked in regular git.
# Deployed services download them from DO Spaces at build time (see Dockerfiles).
# Run `python3 scripts/spaces_sync.py` after pipeline runs to keep Spaces current.

bronze/governance/agora/fetch.sh

# Vector store export intermediates
docs/vector-store-exports/_screenshots_tmp/
docs/vector-store-exports/_image_urls.json
docs/vector-store-exports/.auto-file-ids

# Agora CSVs — too large for git (178 MB Transfer.csv exceeds GitHub 100 MB limit).
# Hosted on DO Spaces. Download with: python3 scripts/spaces_sync.py --download
bronze/governance/agora/**/*.csv

# dbt runtime artifacts
infra/dbt/target/
infra/dbt/dbt_packages/
infra/dbt/logs/

# Warehouse (DuckDB) — .wal is temp write-ahead log, not data
warehouse/*.duckdb.wal

# Great Expectations
infra/great_expectations/uncommitted/
infra/great_expectations/data_docs/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Note: uv.lock IS tracked (committed for reproducibility — do not add it here)

# Linting / tooling
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Claude Code
.claude/

# Internal dev notes
DEV/
TODOS.md

# Node (if any tooling uses it)
node_modules/

# Misc
List of Data Sources.csv
skills
skills-lock.json
.gstack/
